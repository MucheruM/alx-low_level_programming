#include "main.h"

/**
 * main - Print the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 97; a < 123; a++)
	{
		_putchar('a');
	}
	_putchar('\n');
	return (0);
}
